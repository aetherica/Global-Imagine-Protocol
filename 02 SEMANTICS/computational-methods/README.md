# Computational Methods Semantic Layer

**Status:** CLOSED — CURRENT V3.1 COMPUTATIONAL-METHOD NUCLEUS
**Version:** 1.0.0

The layer owns implementation-independent computational methods. The current canonical nucleus is Algorithm.

## Canonical entry

- `SEM-COMPUTATIONAL-METHOD-ALGORITHM-001` — Algorithm — Active

## Boundary

Algorithm ≠ Software implementation ≠ Processing activity ≠ Execution ≠ Result.

`Algorithm → Software Implementation → Execution / Processing → Result` describes realization and execution, not semantic identity.

Visitor Universe controls entry depth into the same method semantics.

Future computational methods require independent semantic validation.
